import {makeProject} from '@motion-canvas/core';

import scene from '../scenes/setup?scene';

import audio from '../audio/BC - 02 - Setup Fixed.mp3'

export default makeProject({
  scenes: [scene],
  audio: audio
});
